animations {
    enabled = true
# Movimento suave para elementos de interface
bezier = cinematic, 0.12, 0.85, 0.18, 1.00

# Fechamento rápido porém elegante
bezier = exit, 0.55, 0.00, 0.75, 0.20

animation = layersIn, 1, 5.3, cinematic, popin 94%

animation = layersOut, 1, 2.8, exit, fade

animation = fadeLayers, 1, 6, soft
animation = fadeLayersIn, 1, 2.4, soft
animation = fadeLayersOut, 1, 2.1, soft

animation = windowsIn, 1, 5.0, overshoot, gnomed

animation = windowsOut, 1, 2.6, exit, gnomed

animation = windowsMove, 1, 4.8, overshoot

animation = workspaces, 1, 5.4, overshot, slidevert

animation = specialWorkspace, 1, 3.5, specialWorkSwitch, slidefadevert 15%

animation = fade, 1, 7, standard

animation = fadeDim, 1, 7, standard
}
